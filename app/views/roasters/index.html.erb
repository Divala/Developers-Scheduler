<h1>Listing roasters</h1>

<table>
  <tr>
    <th>Developers</th>
    <th>Date</th>
  </tr>

<% @roasters.each do |roaster| %>
  <tr>
    <td><%=h roaster.developers %></td>
    <td><%=h roaster.date %></td>
    <td><%= link_to 'Show', roaster %></td>
    <td><%= link_to 'Edit', edit_roaster_path(roaster) %></td>
    <td><%= link_to 'Destroy', roaster, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New roaster', new_roaster_path %>